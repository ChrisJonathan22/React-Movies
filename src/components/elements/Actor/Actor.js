import React from 'react';
import { IMAGE_BASE_URL } from '../../../config';
import './Actor.scss';

const Actor = (props) => {
    return (
        <div>
            Actor
        </div>
    );
};

export default Actor;